AiAnalysis.vue
這頁的目的是：抓 AI 分析並輸出成 md
觸發點是 button -> fetchReport
fetchReport 只做三件事：打 API / 判斷 error / 成功就下載
後端回來的是 { code, report }
report 是 Markdown 字串，不是檔案
下載是用 Blob + 偽造 a.click()
真正開始下載的是 link.click()
檔名由 buildMdFilename 組出
loadingReport 防止重複下載
這條線現在是「完成狀態」